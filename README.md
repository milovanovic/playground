Simple DSP chain testing
=================

Testing:

* Queue (buffer) -> 8bit/32bit adapter -> sdf-fft (status - succesfully tested)
* sdf-fft -> LogMagMux -> Queue (buffer) ->  32bit/8bit adapter (status - work in process)
